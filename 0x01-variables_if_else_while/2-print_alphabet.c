#include<stdio.h>
/**
 * main - prints the alphabet in lowercase
 * followed by the new line
 * Return: Alway 0 (Success)
 */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar('\n');
	}
	putchar('\n');
	return (0);
}

