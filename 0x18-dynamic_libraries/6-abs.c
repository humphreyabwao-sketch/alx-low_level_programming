#include "main.h"
/**
 * _abs - computes the absolute value of an inter
 *
 * @n: Integer computed
 *
 * Return: The absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
